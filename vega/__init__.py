"""Vega Scene program mirror.

A small service that pulls the Vega Scene cinema program into a local SQLite
store and keeps it up to date, so the whole upcoming schedule can be queried in
one place instead of paging through the website one day at a time.
"""
